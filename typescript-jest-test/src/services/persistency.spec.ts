describe('TESTANDO ALGUMA coisa', () => {
  it('should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('TESTANDO ALGUMA coisa', () => {
  test('should return Tiago', () => {
    const nome = 'Tiago';
    expect(nome).toBe('Tiago');
  });
});
